from typing import List

class Solution:
    def findGCD(self, nums: List[int]) -> int:
        def gcd(a, b):
            while b != 0:
                a, b = b, a % b
            return a
        
        mini = min(nums)
        maxi = max(nums)
        return gcd(mini, maxi)
