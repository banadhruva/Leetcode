class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        my_dict={}
        n=len(nums)
        majority_count=n//2

        for i in nums:
            my_dict[i]=my_dict.get(i,0)+1
            if my_dict[i]>majority_count:
                return i






        
