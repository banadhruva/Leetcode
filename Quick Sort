def partition(high, low, nums):
    pivot=nums[low]
    i=low, j=high
    while i<=pivot and i<=high-1:
      i+=1
    while j>=pivot and j>=low+1:
      j-=1
    if i<j:
       nums[i],nums[j]=nums[j],nums[i]
    nums[j], nums[low]=nums[low], nums[j]
    return j

def quick_sort(arr,low, high):
   if low<high:
      p_ind=partition(high,low,arr)
      quick_sort(arr,low,p_ind-1)
      quick_sort(arr,p_ind+1,high)



      
